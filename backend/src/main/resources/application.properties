# ============================================================
# SE1020 Movie Platform – REST API Configuration (SOA)
# ============================================================

# Server
server.port=7000
server.servlet.context-path=/

# MongoDB Configuration – reads from Render/System env, defaults to CineVault Atlas Cluster
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://admin:Admin1234@movierental.x7dnnb1.mongodb.net/cinevault?retryWrites=true&w=majority&appName=MovieRental}

# Embedded Tomcat
server.tomcat.max-threads=200
server.tomcat.connection-timeout=5000

# CORS – Angular dev server
app.cors.allowed-origins=http://localhost:4200

# Jackson
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC

# Seed data into MongoDB on first run
app.seed.enabled=true

# ============================================================
# Gmail SMTP – Invoice Email
# Get App Password: myaccount.google.com/apppasswords
# ============================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=finance.cinevault@gmail.com
spring.mail.password=${SPRING_MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# ============================================================
# Vercel AI Gateway – Review Summarizer
# ============================================================
ai.gateway.url=https://ai-gateway.vercel.sh/v1/chat/completions
ai.gateway.api-key=${AI_GATEWAY_API_KEY:}
ai.gateway.model=openai/gpt-4o-mini